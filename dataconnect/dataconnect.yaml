specVersion: "v1"
serviceId: "vercel"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vercel-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
